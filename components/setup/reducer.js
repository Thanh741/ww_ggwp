
const initialState = {

}
const setupReducer = (state, action) => {
  switch (action.type) {
    case 'SAVE_PLAYER_NUMBER':
      return {}
    default:
      return {}
  }
}

export default setupReducer
